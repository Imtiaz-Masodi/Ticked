const CircularLoader = () => {
  // ToDo: Add Size variants
  return (
    <div className="h-full min-h-6 max-h-32 aspect-square border-4 rounded-full border-t-4 border-t-[currentColor] m-auto animate-spin"></div>
  );
};

export default CircularLoader;
