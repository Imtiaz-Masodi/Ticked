export enum Size {
  sm = "sm",
  md = "md",
  lg = "lg",
}

export enum Priority {
  low = "Low",
  medium = "Medium",
  high = "High",
}

export enum ApiResponseStatus {
  success = "success",
  failed = "failed",
}
